{
  "name": "projects",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "biome check --apply ./"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^1.13.2",
    "@biomejs/biome": "1.7.0",
    "@repo/typescript-config": "workspace:*"
  }
}
